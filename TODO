* aqua core:
	- relay -> finish relay begin (missing ack). extend + extended. also need parse/ip6-to-bin to make this work.
	- relay -> need to implement the digest.
	- circuit mgr -> cell protocol: single path, multipath.

* testing:
	- automatic network simulation testing & benchmarking.
	- automatic unit testing & benchmarking.

* libs:
	- find a good logging module
	- fix stuff in nodedtls: API (host in opts), remoteAddr.




* misc
	- role/roles, make is? is-not? in misc. 
	- merge & clean conv, parse & misc into tools.
	- support multiple roles.
	- try catch everything
	- n-
